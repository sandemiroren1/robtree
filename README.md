# Robtree

Under construction

You need to have vcpkg installed.

Do 
```zsh
vcpkg install && cd src 
```

To configure cmake 

```zsh 
cmake --preset=default
```

To build:

```zsh
cmake --build build
```

